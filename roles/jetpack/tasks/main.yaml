---
- name: Update apt package index
  ansible.builtin.apt:
    update_cache: true

- name: Install the full NVIDIA JetPack meta-package
  ansible.builtin.apt:
    name: nvidia-jetpack
    state: present
  notify: Reboot Jetson
